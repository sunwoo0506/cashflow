# 자금관리 — 프로젝트 규칙

이 파일은 Claude Code가 매 세션 처음 읽는 규칙서다. 코드를 쓰기 전에 여기부터 지킨다.

## 무엇을 만드는가

샘플 그룹 두 법인(법인A · 법인B)의 **주간 자금 현황과 런웨이**를 보는 웹앱.
"현금이 언제 바닥나는가"에 답하는 것이 유일한 목적이고, 나머지 기능은 전부 그 답을 정확하게 만들기 위한 보조다.

이미 동작하는 단일 HTML 프로토타입(`legacy/index.html`)이 있다. **계산 결과가 그것과 달라지면 버그다.**

## 스택

```
Next.js 15 (App Router) · TypeScript · Tailwind + shadcn/ui
Supabase (Postgres · Auth · RLS · Storage) · TanStack Query
SheetJS(xlsx) · Recharts · Vitest · Vercel
```

사내 다른 서비스와 같은 계열로 맞춘다. 나중에 계정·데이터를 붙일 때 비용이 거의 0이 되게.

## 디렉터리

```
apps/web/                     Next.js 앱
packages/cashflow-engine/     ★ 계산 엔진 (순수 TS, 의존성 0)
packages/ui/                  공용 컴포넌트 (사내 다른 서비스와 공유 예정)
supabase/migrations/          스키마
docs/                         이 문서들
legacy/index.html             기존 프로토타입 (기준 동작·골든 테스트 원본)
```

## 절대 규칙

1. **계산 엔진은 `packages/cashflow-engine` 안의 순수 함수다.**
   React·Supabase·fetch·Date.now()를 import 하지 않는다. 입력은 인자로만 받고, 기준일도 인자다.
   UI·서버·테스트가 같은 함수를 부른다.

2. **골든 테스트를 통과하지 않은 계산 변경은 머지하지 않는다.**
   `docs/05-CALC-ENGINE.md`의 기대값이 기준이다. 값이 바뀌어야 한다면 왜 바뀌는지 PR에 적고 기대값을 같이 고친다.

3. **집계를 저장하지 않는다.** 연령분석·잔액·수금상태는 항상 계산으로 뽑는다.
   과거에 매출액을 연령분석 값으로 저장해 두어 실제보다 26배 큰 숫자가 화면에 나온 적이 있다.

4. **추정한 값은 추정이라고 표시한다.** 회수예정일을 회전일로 추정했다면 UI에 그렇게 쓴다.
   숫자만 보여주고 출처를 숨기지 않는다.

5. **멀티테넌시는 처음부터.** 모든 도메인 테이블에 `org_id` + RLS. 나중에 붙이면 마이그레이션이 지옥이 된다.

6. **금액은 `numeric(18,2)`, 코드에서는 정수 원 단위.** float 누적 금지.

7. **한국어 UI.** 회계 용어 대신 누구나 아는 말을 쓴다 (「기수금액」 ✗ → 「받은 금액」 ✓).

## 용어 (코드와 UI에서 같은 뜻으로만 쓴다)

| 용어 | 뜻 | 코드 |
|---|---|---|
| 구분 | 무엇을 파는가 — 일반매출 / 지원사업 | `receivable_kind` |
| 청구상태 | 세금계산서를 끊었는가 — 청구전 / 청구완료 | `receivables.stage` |
| 현재상태 | 얼마나 받았는가 — 미수금 / 일부 수금 / 수금완료 (자동) | `receivables.status` |
| 확률 | 이 영업 건이 계약될 가능성 (0~1) | `opportunities.win_rate` |
| 달성률 | 그 주 계획액 중 실제로 들어온다고 보는 비율 | `achievement_rate` |
| 잔액 | 청구금액 − 받은 금액 | `amount_open` |
| 런웨이 | 현금이 바닥나기까지 남은 주 수 | — |

「확도」·「기수금액」·「신규매출(구분값으로서)」은 쓰지 않는다.

## 작업 방식

- 계산 로직을 건드렸으면 `pnpm test` 결과를 PR에 붙인다.
- 화면을 만들었으면 스크린샷(라이트·다크·모바일 390px)을 붙인다.
- 스키마를 바꿨으면 마이그레이션 파일과 롤백 방법을 같이 낸다.
- 확실하지 않으면 추측해서 만들지 말고 물어본다. 특히 회계 처리 기준.
