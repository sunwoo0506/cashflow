# 2026-09-07 · 로그인·멀티테넌시, 파일 업로드, 배포

---

# 1부 · 개발자용

## 1.1 배포하며 드러난 것 — 배포 URL 은 데이터를 못 막는다

배포 후 공개 URL 이 인증 없이 200 을 돌려주고 화면에 데이터가 그대로 나왔다.

```bash
$ curl -s https://<production-alias> | grep -c "자금 현황"
1        # 🚨 누구나 볼 수 있음
```

플랫폼의 접근 제한을 켜려 했으나 거절됐다.

```
Vercel Authentication is not available on your plan for production deployments
```

미리보기 배포는 302 로 막히는데 **프로덕션 별칭만 공개**였다.

```
https://<deployment-hash>.vercel.app   → 302  (보호됨)
https://<production-alias>.vercel.app  → 200  (공개)  🚨
```

즉시 배포를 내렸다. 그리고 방향을 바꿨다 — **플랫폼이 아니라 앱이 막는다.**

> 결과적으로 이게 옳은 구조다. 이 서비스는 여러 업체가 각자 로그인해 쓰는 것이므로 어차피 앱 레벨 인증이 필요했다. 플랫폼 기능으로 막았다면 「전부 공개 아니면 전부 차단」 뿐이라 업체별 격리가 불가능했다.

## 1.2 인증

비밀번호 없는 이메일 로그인. 링크를 누르거나 코드를 입력한다.

```
lib/supabase/client.ts    브라우저 (anon 키 — 공개돼도 RLS 가 막는다)
lib/supabase/server.ts    서버 컴포넌트·액션 (쿠키의 세션으로 질의)
middleware.ts             세션 갱신 + 경로 보호
```

미들웨어에서 `getUser()` 를 부르는 이유: `getSession()` 은 만료된 토큰을 갱신하지 않는다.

```ts
// 갱신까지 하려면 getUser()
const { data: { user } } = await supabase.auth.getUser();
if (!user && !isPublic) return NextResponse.redirect(loginUrl);
```

## 1.3 멀티테넌시 — 스키마엔 있는데 입구가 없었다

`org_id` 와 RLS 는 처음부터 있었다. 그런데 **회사를 만들 방법이 없었다.**

```sql
-- 있던 것
create policy organizations_read on organizations for select
  using (id in (select app_org_ids()));   -- 내가 속한 회사만 조회

-- 없던 것
--   organizations INSERT 정책
--   memberships   INSERT 정책
```

가입한 사람이 첫 회사를 만들 경로가 아예 없었다. 정책으로 열지 않고 `security definer` 함수로 연 이유:

> 회사 생성은 `organizations` INSERT 와 `memberships` INSERT 가 **한 트랜잭션에서 같이** 일어나야 한다. 정책만 열면 그 사이에 실패했을 때 주인 없는 회사가 남는다.

```sql
create or replace function fn_create_organization(p_name text, p_slug text default null)
returns organizations
language plpgsql security definer as $$
begin
  if auth.uid() is null then raise exception '로그인이 필요합니다'; end if;
  insert into organizations (name, slug) values (...) returning * into v_org;
  insert into memberships (org_id, user_id, role) values (v_org.id, auth.uid(), 'owner');
  return v_org;
end $$;
```

한글 회사 이름은 라틴 슬러그로 못 바꾸므로, 남는 게 없으면 임의 문자열을 쓰고 충돌 시 번호를 붙인다.

## 1.4 「한 회사 고정」 걷어내기

하드코딩된 회사·법인을 전부 제거했다.

| 전 | 후 |
|---|---|
| 제목 고정 | 로그인한 사람의 회사 이름 (여럿이면 전환 드롭다운) |
| 법인 배열 상수 | 회사 설정에 등록한 법인 |
| 픽스처 파일 | 회사별 DB 조회 (RLS 가 서버에서 재차단) |

데이터 계층을 둘로 나눴다.

```
lib/fixture-dataset.ts    샘플 Dataset (테스트·시딩용, DB 불필요)
lib/data.ts               회사별 DB 조회 (server-only)
```

`Dataset` 모양이 같으므로 화면 컴포넌트는 손대지 않았다.

### 이 과정에서 잡은 버그 — URL 공유가 반쪽이었다

법인 필터만 URL 로 복원되지 않았다.

```ts
// ❌ 미리 인코딩해 두고, 자동 디코딩된 값과 비교 → 영영 안 맞음
const slug = (n: string) => encodeURIComponent(n.replace(/[()\s]/g, ''));
params.get('corp') === slug(entity)   // "법인A" vs "%EB%B2%95..."

// ✅ URLSearchParams 가 넣을 때 인코딩하고 꺼낼 때 디코딩한다
const slug = (n: string) => n.replace(/[()（）\s]/g, '').slice(0, 24);
```

### 스키마와 문서가 어긋난 곳

추측하지 않고 기록만 남겼다.

| 문서 | 실제 SQL | 어떻게 처리했나 |
|---|---|---|
| `receivables.stage` (청구전/청구완료) | 그런 컬럼 없음 | 「세금계산서를 끊었는가」이므로 `issued_on` 유무로 판별 |
| 마이너스 잔액 11건 | `amount_open >= 0` 제약 | DB 로는 표현 불가 — 선수금·반품 모델이 없다 |

## 1.5 파일 업로드

부서마다 양식이 다르다. **헤더로 자동 추측하되 사람이 고칠 수 있게** 만들었다.

```
파일 → 시트·종류 선택 → 열 자동 맞춤(수정 가능) → 검사 → 오류 뺀 나머지 반영
                                                            ↓
                                              원본 행은 그대로 보관
```

자동 매핑은 2단계로 찾는다.

```ts
// 1) 별칭과 정확히 같은 헤더  2) 별칭을 포함하는 헤더
// 못 찾으면 비워 둔다 — 틀린 채로 넣는 것보다 사람이 고르는 게 낫다
```

검사 코드: `MISSING_DUE_ON` · `BAD_DATE_FORMAT` · `AMOUNT_NOT_NUMBER` · `UNKNOWN_ENTITY` · `DUP_EVIDENCE_NO` · `SHARE_SUM_NOT_100` · `COLLECTED_GT_BILLED` · `DEFER_NEEDS_DATE`

날짜는 엑셀 일련번호·`2026-08-13`·`2026/8/13`·`20260813` 을 모두 읽는다. 못 읽으면 **추측하지 않고 오류로 돌려준다.**

원본 행을 `submission_rows.raw` 에 그대로 남기므로 파싱 규칙이 바뀌면 다시 돌릴 수 있다.

## 1.6 샘플 데이터 버튼

빈 회사에서는 기능을 볼 수 없다. 버튼 하나로 가상 데이터 한 벌을 넣는다.

```ts
// 이미 데이터가 있으면 덮지 않는다 — 실제 원장을 지우면 안 된다
if ((count ?? 0) > 0) return { ok: false, message: `이미 채권 ${count}건이 있습니다` };
```

법인 → 거래처 → 채권 → 파이프라인 → 고정비(+배분) → 지출 → 가정값 순으로 넣는다.

## 1.7 기대값을 손으로 적지 않기

골든 테스트의 기대값을 코드에 박아 두면, 데이터가 바뀔 때마다 수십 개를 손으로 고쳐야 한다.

```
__fixtures__/golden-expected.json   ← 스냅샷 (자동 생성)
test/expected.ts                    ← 타입 붙여 읽기
GEN=1 pnpm test                     ← 갱신
```

테스트는 **절대값과 불변식을 같이** 본다. 절대값은 스냅샷에서 읽고, 불변식은 코드에 남긴다.

```ts
expect(r.totalIn).toBe(c.totalIn);                    // 절대값 (스냅샷)
expect(r.endCash).toBe(opening + r.totalIn - r.totalOut);  // 불변식 (영구)
```

> 값이 바뀌면 계산이 달라졌다는 뜻이다. 확인 없이 갱신하는 것은 테스트를 지우는 것과 같다.

## 1.8 최종 상태

```
엔진 테스트 88 · 화면 테스트 21 · 커버리지 99%
스키마 21테이블 · RLS 21/21 · 정책 37 · 뷰 7 · 함수 12
배포 : 루트 → 307 → /login (데이터 노출 0)
```

---

# 2부 · 쉽게 보기

## 오늘 가장 중요한 발견

```mermaid
flowchart TD
    A["🌐 배포했다"] --> B["😱 링크만 알면<br/>누구나 열림"]
    B --> C{"어떻게 막지?"}
    C --> D["🔒 플랫폼 기능<br/>→ 유료라 못 씀"]
    C --> E["🔑 앱에 로그인 붙이기<br/>→ 이게 정답"]
    style B fill:#fdeaea,stroke:#c02b2b
    style E fill:#e6f4ea,stroke:#0f7a3d
```

**결과적으로 잘 됐다.** 어차피 업체마다 따로 쓰는 서비스라 로그인은 필요했다.
플랫폼으로 막았으면 「전부 공개 or 전부 차단」 뿐이라 업체별 구분이 안 된다.

## 어떻게 들어오나

```mermaid
flowchart TD
    A["📧 이메일 입력"] --> B["📬 메일의 링크 클릭"]
    B --> C{"내 회사가<br/>있나?"}
    C -- 없음 --> D["🏢 회사 만들기"]
    C -- 있음 --> E["📊 우리 회사 자금 화면"]
    D --> E
    style A fill:#e8f3ec,stroke:#1f8a4c
```

비밀번호가 없다. 외울 것도, 잃어버릴 것도 없다.

## 회사끼리 안 섞이는 이유

```mermaid
flowchart TD
    U1["👤 A사 직원"] --> DB[("데이터베이스")]
    U2["👤 B사 직원"] --> DB
    DB --> R1["A사 것만 보임"]
    DB --> R2["B사 것만 보임"]
    style DB fill:#fdf3e0,stroke:#a86a00
```

데이터베이스가 **직접** 막는다. 화면 코드가 실수해도 남의 회사 데이터는 안 나온다.

## 있었는데 없었던 문
> 「우리 회사만 볼 수 있다」는 규칙은 있었는데,
> **회사를 처음 만드는 문**이 없었다.

```
🚪 들어가는 문 : 있음  (내 회사만 조회)
🔨 짓는 문     : 없음  (첫 회사 생성)   ← 오늘 만듦
```

## 파일 올리기 — 대신 읽어주되, 확인은 사람이

```mermaid
flowchart LR
    A["📄 엑셀"] --> B["🔍 열 이름 보고<br/>자동으로 맞춤"]
    B --> C["👀 맞는지 확인<br/><i>틀리면 고침</i>"]
    C --> D["✅ 검사"]
    D --> E["📥 이상 없는 것만<br/>들여보냄"]
    style C fill:#fdf3e0,stroke:#a86a00
```

**확실하지 않으면 비워 둔다.** 틀린 채로 넣는 것보다 사람이 고르는 게 낫다.

틀린 줄은 이렇게 알려준다.

```
14줄 · 회수예정일 : 「2026년13월」 을 날짜로 읽을 수 없습니다
22줄 · 법인       : 「법인C」 는 회사 설정에 없는 법인입니다
31줄 · 받은 금액  : 받은 금액이 청구금액보다 큽니다
```

## 샘플 데이터 버튼

```mermaid
flowchart LR
    A["🏢 빈 회사<br/>(아무것도 없음)"] --> B["🖱️ 샘플 데이터<br/>적용하기"]
    B --> C["📊 화면이 가득 참<br/>→ 기능 확인 가능"]
    style B fill:#e8f3ec,stroke:#1f8a4c
```

가상 데이터라 실제 거래 정보가 아니다. 이미 데이터가 있으면 **덮어쓰지 않는다.**

## 기대값을 손으로 안 적는 이유

```
❌ 테스트에 숫자를 박아둠
   → 데이터 바뀌면 수십 개를 손으로 고쳐야 함
   → 귀찮아서 대충 맞추다 보면 테스트가 의미를 잃음

✅ 자동으로 뽑아 파일에 저장
   → 값이 바뀌면 "왜 바뀌었지?" 를 먼저 묻게 됨
```

그리고 **절대 안 바뀌는 규칙**은 따로 검사한다.

```
기말 잔고 = 기초 + 들어온 돈 − 나간 돈      ← 데이터가 뭐든 항상 참
6개 버킷 합계 = 받을 돈 총액                ← 데이터가 뭐든 항상 참
```

숫자는 바뀌어도 **규칙은 안 바뀐다.**

---

## 이전 작업 수정 이력

### 2026-09-06 작업을 오늘 고친 것

| 언제 한 것 | 무엇을 고쳤나 | 왜 |
|---|---|---|
| 09-06 · 화면 데이터 로딩 | 런타임 파일 읽기 → **번들에 포함** | 배포 환경에서 파일 경로가 달라 화면이 깨진다 |
| 09-06 · 법인 필터 | 하드코딩 배열 → **회사 설정에서 읽기** | 회사마다 법인이 다르다. 한 회사로 고정할 수 없다 |
| 09-06 · 법인 슬러그 | 퍼센트 인코딩 **제거** | 미리 인코딩한 값과 자동 디코딩된 값이 안 맞아 URL 복원이 실패했다 |
| 09-06 · 화면 제목 | 고정 문자열 → **로그인한 회사 이름** | 업체별로 쓰는 서비스라 회사마다 달라야 한다 |
| 09-06 · 데이터 출처 | 파일 → **회사별 DB 조회** | 데이터를 한 회사로 고정하지 않기 위해 |
| 09-06 · 골든 기대값 | 코드에 박힌 숫자 → **스냅샷 파일** | 데이터가 바뀔 때마다 수십 개를 손으로 고쳐야 했다 |
| 09-06 · 테스트 | 절대값만 → **절대값 + 불변식** | 절대값만 보면 "왜 이 숫자여야 하는지" 를 잃는다 |

### 되돌린 것

| 무엇 | 왜 |
|---|---|
| 첫 배포를 내림 | 인증 없이 데이터가 공개돼 있었다. 로그인을 붙인 뒤 다시 배포했다 |

---
---

# 이어서 · 배포 후 실사용에서 드러난 것들

앞의 기록은 배포까지다. 이 절은 **실제로 써 보면서** 나온 문제와 고친 내용이다.

---

## 1부 · 개발자용

### 1.9 「스키마에 없다」던 컬럼이 있었다 — 어제 판단을 뒤집는다

어제 나는 이렇게 적었다.

> `docs/03` 은 `receivables.stage`(청구전/청구완료)를 말하는데 **SQL 에는 그 컬럼이 없다.**
> 「세금계산서를 끊었는가」이므로 `issued_on` 유무로 판별한다.

**틀렸다.** `stage` 는 존재한다. 0002 에서 나중에 붙는 컬럼이라 0001 의 `create table` 만 보고 없다고 단정한 것이다.

```sql
-- 0002_pipeline_product.sql
alter table receivables add column stage ar_stage not null default '청구완료';
alter table receivables add constraint receivable_stage_evidence
  check (stage = '청구전' or issued_on is not null);
```

한 파일만 보고 「없다」고 결론지은 것이 원인이다. `ALTER TABLE` 로 나중에 붙는 컬럼은
`create table` 블록에 없다 — 스키마를 확인할 때는 **테이블 이름으로 전체를 훑어야 한다.**

이 오판은 두 곳에 남아 있었다.

| 어디 | 무엇이 잘못됐나 |
|---|---|
| 조회 (`lib/data.ts`) | `stage` 를 안 읽고 `issued_on` 유무로 추정 |
| 시딩 | `stage` 를 안 넣어 기본값 `'청구완료'` 가 박힘 → 청구전 행이 제약 위반 |

지금은 컬럼을 그대로 읽고 쓴다. 값이 없는 옛 행만 `issued_on` 으로 채운다.

### 1.10 오류를 `[object Object]` 로 뭉개고 있었다

```
샘플 데이터를 넣지 못했습니다: [object Object]
```

`err instanceof Error ? err.message : String(err)` 로 처리했는데,
**Supabase 오류는 `Error` 인스턴스가 아니다.** `{ message, details, hint, code }` 객체다.
`String()` 을 먹이면 `[object Object]` 가 되어 아무것도 알 수 없다.

```ts
function describe(err: unknown): string {
  if (err instanceof Error) return err.message;
  if (err && typeof err === 'object') {
    const e = err as Record<string, unknown>;
    const parts = [e.message, e.details, e.hint].filter(Boolean).map(String);
    if (parts.length) return parts.join(' · ') + (e.code ? ` (${String(e.code)})` : '');
  }
  return String(err);
}
```

고치자마자 진짜 원인이 줄줄이 드러났다. **오류를 감추면 그 뒤의 오류도 못 본다.**

### 1.11 제약 세 개를 연달아 맞았다

시딩이 스키마 제약을 몰랐다. 하나 고치면 다음 것이 나왔다.

| 제약 | 무엇을 요구하나 | 어떻게 고쳤나 |
|---|---|---|
| `receivable_project_required` | `kind='지원사업'` 이면 과제 필수 | 샘플용 과제를 만들어 묶는다 |
| `receivable_stage_evidence` | 청구완료면 발행일 필수 | `stage` 를 명시해서 넣는다 |
| `opp_needs_counterparty` | 파이프라인은 거래처 이름이나 id 필수 | 건명으로 채운다 |

세 번째는 **업로드 경로에도 같은 결함**이 있었다. 파일에 거래처 열이 없으면 그대로 터졌을 것이다.

그리고 중간에 실패하면 절반만 들어간 채로 남았다. 지금은 실패 시 넣던 것을 되돌린다.

### 1.12 기준일을 「오늘」로 고정한 것이 없던 위험을 만들었다

샘플을 넣자마자 **「위험 · 8월 10일에 통장이 바닥납니다」** 가 떴다.

계산은 규칙대로였다. R3 는 회수예정일이 기준일보다 과거면 기준일 칸으로 당긴다.
기준일을 `오늘` 로 고정해 두었으니, 8월 자료를 9월에 열면 **8월 유입이 전부 9월로 밀려**
8월 초·중순은 유입 0 에 지출만 남는다.

```
자료 기준일 8/13 · 오늘 9/7
❌ 기준일 = 오늘  → 8월 채권이 전부 9월로 당겨짐 → 8월에 바닥
✅ 기준일 = 회차  → 8/13 로 보면 그대로 안전
```

기준일은 **보고 회차의 날짜**지 오늘이 아니다. 스키마에도 `report_periods.as_of` 가 있었는데
쓰지 않고 `todayISO()` 를 박아 두었다. 지금은 회차에서 읽고, 회차가 없을 때만 오늘로 본다.

고치자 샘플이 골든 테스트와 **똑같은 숫자**(안전 · 최저 71,500,000)를 낸다.

### 1.13 화면 단위와 선택 단위가 어긋나 있었다

「월간」으로 바꿔도 상단에는 여전히 **기준주차** 를 고르라고 했다.

기준 시점은 주차 하나로 유지하되 **화면 단위만 바꾸는** 방식으로 정리했다.
둘을 따로 두면 서로 어긋나기 때문이다.

```ts
const monthIndex = result.months.findIndex((m) =>
  m.weeks?.some((w) => w.code === result.weeks[weekIndex]?.code));
const current = gran === 'week' ? result.weeks[weekIndex] : result.months[monthIndex];
```

월간에서는 달을 고르면 그 달의 첫 주로 기준 시점이 옮겨 간다. 타일 라벨도 「주말 잔고」 → 「월말 잔고」로 바뀐다.

### 1.14 그 밖에

- **가정값이 없는 회사의 기본 기간이 22주** 였다. 1월부터 22주면 6월에 끊긴다. 그 해 전체(53주 → 12/31 에서 잘림)로 바꿨다.
- **달성률 슬라이더가 상단 바에** 있었다. 아래에 달성률 칸이 따로 있어 조작이 두 곳에 흩어졌다. 슬라이더는 달성률 칸으로 옮기고, 상단에는 현재 값만 남겼다.
- **샘플을 넣는 길만 있고 지우는 길이 없었다.** 시험 삼아 넣어 볼 수가 없다. 「데이터 비우기」를 넣되 한 번 더 확인을 받는다.
- **회사 설정에서 본 화면으로 돌아가는 길이 위쪽 작은 링크뿐**이었다. 하단에 「자금 현황 보기」를 크게 두었다.

### 1.15 로그인이 막혔던 이유들

메일 발송 한도(무료 플랜)에 걸려 아무도 못 들어오는 상황이 생겼다.

- **비밀번호 로그인을 추가**했다. 로그인은 메일을 보내지 않는다.
- 다만 **가입은 확인 메일을 보낸다.** 「메일을 보내지 않아 한도에 안 걸린다」는 문구를 가입 모드에서도 그대로 보여주고 있었다 — 사실이 아니었다. 모드별로 나눴다.
- 그 뒤 **이메일 제공자 자체가 꺼져** 모든 방식이 막혔다. 원문(`Email signups are disabled`)만으로는 어디를 봐야 할지 알 수 없어, 어느 화면에서 무엇을 켜야 하는지까지 문장으로 돌려주게 했다.

---

## 2부 · 쉽게 보기

### 오류를 감추면 그 뒤가 안 보인다

```mermaid
flowchart LR
    A["샘플 넣기 실패"] --> B["[object Object]"]
    B --> C["😵 뭐가 문젠지 모름"]
    A2["샘플 넣기 실패"] --> B2["과제가 없습니다"]
    B2 --> C2["✅ 고침 → 다음 문제가 보임"]
    style B fill:#fdeaea,stroke:#c02b2b
    style B2 fill:#e6f4ea,stroke:#0f7a3d
```

오류 메시지를 제대로 찍게 고쳤더니 **숨어 있던 문제 3개가 줄줄이** 나왔다.

### 「오늘」을 기준으로 삼으면 안 되는 이유

```
8월 자료를 9월에 열었다

❌ 기준 = 오늘(9월)
   8월에 받기로 한 돈 → "이미 늦었네" → 9월로 밀림
   → 8월엔 나갈 돈만 남음 → 없던 위기가 생김

✅ 기준 = 그 자료의 날짜(8월)
   8월엔 8월대로 → 실제 그때 상황 그대로
```

지난달 보고서를 오늘 열었다고 해서 지난달이 갑자기 위기였던 게 되지는 않는다.

### 월간인데 주를 고르라고 했다

```
[주간] [월간]  ← 월간 선택
기준주차: [W03 · 8월 3주 ▾]   ← 그런데 주를 고르라고 함 ❌
기준 월  : [8월 ▾]            ← 이렇게 바뀌어야 ✅
```

### 넣기만 되고 지우기가 없었다

```mermaid
flowchart LR
    A["샘플 넣기"] --> B["😰 되돌릴 수가 없네"]
    A2["샘플 넣기"] <--> B2["데이터 비우기"]
    B2 --> C2["✅ 마음 놓고 시험"]
    style B fill:#fdeaea,stroke:#c02b2b
    style C2 fill:#e6f4ea,stroke:#0f7a3d
```

### 스키마는 한 파일만 보면 안 된다

```
0001_core.sql          create table receivables (...)      ← 여기만 봄
0002_pipeline.sql      alter table receivables add stage   ← 놓침
                                                   ↑
                                        "그런 컬럼 없다"고 단정
```

테이블은 나중에 컬럼이 붙는다. **이름으로 전체를 훑어야 한다.**

---

## 이전 작업 수정 이력 (이어서)

### 어제(2026-09-06) 작업을 오늘 고친 것 — 추가분

| 언제 한 것 | 무엇을 고쳤나 | 왜 |
|---|---|---|
| 09-06 · 데이터 계층 | 기준일 `오늘` → **보고 회차의 날짜** | 지난 시점 자료를 열면 없던 위험이 만들어졌다 |
| 09-06 · 데이터 계층 | 기본 기간 22주 → **그 해 전체** | 1월부터 22주면 6월에 끊긴다 |
| 09-06 · 전역 컨트롤 | 달성률 슬라이더를 **달성률 칸으로 이동** | 조작이 두 곳에 흩어져 있었다 |
| 09-06 · 전역 컨트롤 | 월간일 때 **월 선택**으로 전환 | 월간인데 주차를 고르라고 했다 |
| 09-06 · 오늘 탭 | 주/월에 맞는 값과 라벨 | 월간에서도 주 숫자를 보여줬다 |

### 오늘 쓴 것 중 오늘 다시 고친 것

| 무엇 | 왜 |
|---|---|
| 「스키마에 `stage` 컬럼이 없다」는 판단 | **틀렸다.** 0002 에서 붙는 컬럼을 못 봤다. 조회·시딩 양쪽을 컬럼 기준으로 되돌렸다 |
| 「비밀번호는 메일을 안 보낸다」는 안내 | **가입은 보낸다.** 모드별로 문구를 나눴다 |
| 샘플 시딩 | 제약 3개(과제·발행일·거래처)를 몰라 실패했다. 실패 시 되돌리기도 없었다 |

---
---

# 이어서(2) · 나갈돈을 「계획」에서 「실제」로

앞 절까지가 화면을 실제로 써 보며 고친 것이라면, 이 절은 **나갈돈 탭 한 곳을 제대로 만든** 기록이다.
요구는 한 줄이었다 — *「고정비 부분도 집행, 연기 부분을 넣어서 실제 집행여부를 확인할 수 있게 해줘」*.

---

## 1부 · 개발자용

### 1.16 고정비에는 「이번 달만」이 없었다

일회성 지출(`expenses`)은 처음부터 건별로 집행상태를 들고 있었다.

```ts
interface Expense { … execState: ExecState; deferToWeek?: string | null }
```

고정비는 아니었다. 고정비는 *항목* 하나가 12개월을 다 대표한다.

```
Expense    1행 = 1건 = 1번의 집행       → 상태를 행에 붙이면 끝
FixedCost  1행 = 매달 반복             → 상태를 행에 붙이면 12달이 같이 바뀐다
```

「9월 임차료만 2주 미룸」을 표현할 자리가 없었다. 상태는 **항목이 아니라 (항목 × 달)** 에 붙는다.

#### 타입

```ts
export interface FixedCost {
  id: string; item: string; amount: number; payDay: number;
  shares: Record<EntityName, number>;
  /** 달마다 집행 상태를 달리 둘 수 있다. 키는 '8월' 같은 달 이름. */
  monthlyState?: Record<string, { execState: ExecState; deferToWeek?: string | null }>;
}
```

선택 필드로 뒀다. **없으면 「집행」** 이므로 기존 골든값이 한 원도 안 움직인다.

#### 스키마 — `0005_fixed_cost_runs.sql`

```sql
create table fixed_cost_runs (
  id uuid primary key default gen_random_uuid(),
  org_id uuid not null references organizations(id) on delete cascade,
  fixed_cost_id uuid not null references fixed_costs(id) on delete cascade,
  month_start date not null,
  exec_state exec_state not null default '집행',
  deferred_to date, note text,
  …
  unique (fixed_cost_id, month_start),
  constraint fcr_defer_needs_date  check (exec_state <> '연기' or deferred_to is not null),
  constraint fcr_month_is_first    check (extract(day from month_start) = 1)
);
```

제약 두 개가 이 테이블의 요지다.

| 제약 | 막는 것 |
|---|---|
| `fcr_defer_needs_date` | 「미뤘다」면서 **어디로** 미뤘는지 없는 행 |
| `fcr_month_is_first` | `2026-09-15` 같은 애매한 「달」 — 달은 1일 하나로만 |
| `unique (fixed_cost_id, month_start)` | 한 달에 상태가 두 개 |

`fixed_costs` 를 지우면 `on delete cascade` 로 같이 지워진다 — 초기화 버튼에 손댈 필요가 없었다.

### 1.17 배치 규칙을 두 번 쓰지 않기

집행/연기/취소는 이미 일회성 지출에 있었다. 고정비에 같은 걸 다시 쓰면 **언젠가 갈라진다.**
그래서 `placeOutflows` 안의 두 갈래를 하나의 내부 함수로 합쳤다.

```
      고정비 루프 ─┐
                   ├─▶ place(base, srcSeg, execState, deferToWeek, bucket)
      지출 루프 ───┘         │
                             ├ 취소 → 원래 칸에 기록만, 합계에서 제외
                             ├ 연기 → 원래 칸에서 빼고 「연기」, 간 곳에 「이월」
                             └ 집행 → 그대로
```

`bucket` 만 `'fixedCost' | 'expense'` 로 달라진다. 규칙은 한 벌이다.

호출부:

```ts
const state = f.monthlyState?.[`${m}월`];
place({ kind: '고정비', name: f.item, entity: input.entityFilter ?? '전체',
        amount, date: toISO(d), expenseId: f.id },
      srcSeg, state?.execState ?? '집행', state?.deferToWeek ?? null, 'fixedCost');
```

### 1.18 테스트 — 새 기능은 골든이 안 지켜준다

골든 픽스처에는 `monthlyState` 가 없다. 즉 **G1~G9 를 다 통과해도 이 코드는 한 줄도 안 돈다.**
G9(일회성 연기·취소)와 같은 모양으로 G10 을 따로 세웠다.

```ts
const delta = (r) => { /* 기본 대비 주별 fixedCost 변동만 뽑는다 */ };
```

합계만 보면 **연기가 검증되지 않는다** — 기간 안에서 옮기면 총액은 그대로이기 때문이다.
그래서 주별 변동을 본다.

| 검증 | 기대 |
|---|---|
| 상태 미기재 | 기존 결과와 완전 동일 (`delta = {}`) |
| 연기 기본 | `W(n)` 에서 −8,500,000 · `W(n+4)` 에서 +8,500,000 · 총액 불변 |
| 연기 주차 지정 | 지정한 `W22` 로 |
| 취소 | 총유출 −8,500,000 · `cancelled` 에 기록 |
| 연기 `out` | 총유출 −8,500,000 · `deferredOutOfRange` 에 기록 |
| 한 달만 변경 | 다른 달 변동 없음 · 두 달 취소 시 정확히 2배 |
| 다른 항목 | 집행 건수 차이가 정확히 1 |

`96 passed`(엔진) + `23 passed`(웹). 기존 골든값은 그대로다.

### 1.19 데이터 계층 · 화면

`lib/data.ts` 가 `fixed_cost_runs` 를 읽어 달 이름 키로 접는다.

```ts
const month = `${Number(String(r.month_start).slice(5, 7))}월`;
cur[month] = { execState: r.exec_state,
               deferToWeek: r.deferred_to ? weekCodeOf(r.deferred_to) : null };
```

날짜를 **주차 코드로 바꾸는 것은 데이터 계층의 일**이다. 엔진은 주차 코드만 안다.

화면(`components/tabs/outflows.tsx`)은 **DB 를 다시 읽지 않는다.** 엔진이 배치한 결과에서 읽는다.

```ts
for (const w of s.result.weeks)
  for (const o of w.outflows) {
    if (o.kind !== '고정비' || !o.expenseId) continue;
    if (w.month === month && o.placement !== '이월') m.set(o.expenseId, { … });
  }
```

`이월` 을 뒤로 미룬 이유: 9월에 미룬 임차료가 10월에 「이월」로 잡히는데,
**그 달의 상태**는 원래 자리 기준(연기/취소/집행)이어야 하기 때문이다. 이월은 *받은 쪽* 표시로만 쓴다.

### 1.20 샘플에 예시를 심었다

기록이 하나도 없으면 화면이 전부 「집행」으로만 보여서, 이 기능이 있는지조차 알 수 없다.
샘플 적용 시 이번 달치로 두 건을 넣는다 — 임차료 **연기**(지급일 +14일), 차량유지·유류 **취소**.

`deferred_to` 는 제약 때문에 반드시 채워야 하므로 지급일에서 2주 뒤로 계산해서 넣는다.

---

## 2부 · 쉽게 보기

### 고정비는 「매달 같은 날 나가는 돈」이다

```
임차료 850만 · 매달 1일

  8월 1일   9월 1일   10월 1일   11월 1일
    ▼         ▼         ▼          ▼
   850만     850만      850만      850만
```

그런데 실제로는 이런 일이 생긴다.

```
  9월 1일 ── 건물주와 협의, 2주 미룸 ──▶ 9월 15일
 10월 1일 ── 이번 달은 안 나감 (취소)
```

전에는 이걸 적을 곳이 없었다. **항목 하나에 상태 하나**뿐이라, 9월을 미루면 8월·10월·11월이 전부 같이 미뤄졌다.

### 그래서 「달마다」로 바꿨다

```
        전                              후
  ┌───────────┐              ┌───────────┬──────┐
  │  임차료   │ 집행          │  임차료   │      │
  └───────────┘              ├───────────┼──────┤
        ▲                    │   8월     │ 집행 │
   한 칸뿐                   │   9월     │ 연기 │ → 9/15
                             │  10월     │ 취소 │
                             │  11월     │ 집행 │
                             └───────────┴──────┘
```

### 화면에서는 이렇게 보인다

```
 고정비 내역 · 법인A                       9월 집행
 ────────────────────────────────────────────────────
 임차료         시설    매월 1일   [연기] → W07   425만
 급여           인건비  매월 25일  [집행]        4,140만
 차량유지·유류  차량    매월 15일  [취소]         112만   ← 흐리게
 보험료         보험    매월 10일  [집행]          90만
```

- **[집행]** 그대로 나갔다
- **[연기] → W07** 이 자리에서 빠져서 7주차로 갔다
- **[이월] W03에서** 다른 달에서 넘어온 것
- **[취소]** 이번 달은 안 나간다 (금액을 흐리게 — 합계에 안 들어간다)

상단에서 **달을 바꾸면** 그 달 기준으로 바뀐다.

### 돈은 사라지지 않는다

```
  연기                              취소 / 기간 밖
  ───────────────────────           ─────────────────
  W03 ──850만──▶ W07               W03 ──850만──▶ ✕
  총 나갈 돈: 그대로                 총 나갈 돈: 850만 줄어듦
  (자리만 옮김)                      (기록은 남는다)
```

「미룬 것」과 「안 쓴 것」은 **런웨이에 주는 영향이 다르다.** 그래서 구분해서 센다.

### 새 기능은 기존 시험이 안 잡아준다

```
  기존 골든 테스트 G1~G9
  └─ 「상태 기록」이 데이터에 아예 없음
     └─ 새로 쓴 코드가 한 줄도 안 돌아감
        └─ 다 통과해도 안전하지 않음  ✕

  그래서 G10 을 따로 세움
  └─ 미룸·건너뜀·다른 달 영향까지 8가지를 직접 태움  ✓
```

그리고 **합계만 보면 안 된다** — 기간 안에서 미루면 총액은 똑같기 때문이다.
「어느 주에서 빠져서 어느 주로 갔는지」를 봐야 미룬 것이 검증된다.

---

## 이전 작업 수정 이력 (이어서 2)

### 오늘 앞부분에 쓴 것을 오늘 다시 고친 것

| 언제 한 것 | 무엇을 고쳤나 | 왜 |
|---|---|---|
| 09-07 · 엔진 `outflow.ts` | 고정비·지출의 배치 코드를 **하나의 `place()` 로 합침** | 같은 규칙을 두 벌 두면 갈라진다. 고정비에 연기·취소를 붙이며 통합 |
| 09-07 · 샘플 시딩 | 고정비 집행상태 예시 2건 **추가 삽입** | 기록이 없으면 화면에 기능이 있는지 알 수 없다 |

### 아직 안 한 것

| 무엇 | 왜 미뤘나 |
|---|---|
| 화면에서 집행상태 **바꾸기** | 지금은 보기만 된다. 쓰기는 권한·감사기록을 같이 설계해야 한다 |
| 고정비 **성격** 값 받기 | 지금은 항목 이름으로 추정하고 「추정」 배지를 단다. 양식에 칸이 생기면 그 값이 이긴다 |

---
---

# 이어서(3) · 「자료가 샜다」로 보인 것 — 실제로는 안 샜다

**신고**: 다른 계정으로 로그인했는데 앞 계정의 샘플 데이터와 법인명이 그대로 보인다.

멀티테넌시 사고 신고이므로, 추측하지 않고 **DB 를 직접 열어 확인**했다.

---

## 1부 · 개발자용

### 1.21 확인 절차 — 코드가 아니라 데이터를 봤다

코드만 읽으면 「RLS 가 있으니 괜찮다」로 끝난다. 그건 확인이 아니다.

```
1) 익명 REST 로 각 테이블을 긁어 본다      → 새는 곳이 있으면 여기서 나온다
2) 회사·멤버십·회사별 건수를 센다           → 누가 어느 회사에 속했나
3) 행 id 가 회사끼리 겹치는지 센다          → 같은 행을 공유하나
4) RLS 미적용 · 정책 없는 테이블을 찾는다    → 구멍이 있나
```

#### 1) 익명 접근

```
receivables   []      fixed_costs  []
entities      []      expenses     []
organizations []
```

#### 2) 회사와 멤버

| 회사 | 소유자 | 채권 | 고정비 | 지출 | 법인 |
|---|---|---|---|---|---|
| 회사 ① | 계정 A | 106 | 9 | 55 | 법인A · 법인B |
| 회사 ② | 계정 B | 106 | 9 | 55 | 법인A · 법인B |
| 회사 ③ | 계정 C | 106 | 9 | 55 | 법인A · 법인B |

#### 3) 행이 겹치는가

```sql
select count(*) from (
  select id from receivables group by id having count(distinct org_id) > 1) x;
-- 0
```

채권 총 **318 = 106 × 3**. 법인 행도 회사마다 다른 uuid 다.

#### 4) 구멍

`public` 스키마에 **RLS 가 꺼진 테이블 0개 · 정책이 없는 테이블 0개.**

### 결론 — 새지 않았다. 다만 **똑같이 생겼다**

```
   보이는 것                        실제
   ─────────────                    ────────────────────────
   같은 화면                        회사 ①  ┌ 106건 (org_id=①)
   같은 법인명           ≠          회사 ②  ┌ 106건 (org_id=②)
   같은 금액                        회사 ③  ┌ 106건 (org_id=③)
                                    서로 겹치는 행: 0
```

샘플 데이터는 **고정 시드로 만든 같은 픽스처**다. 어느 회사에 넣어도 채권 106건·고정비 9건·
지출 55건·법인 「법인A·법인B」가 똑같이 나온다. 계정 셋이 각각 「샘플 적용」을 눌렀으니
세 화면이 픽셀 단위로 같아진다.

**이건 버그가 아니지만 결함이다.** 자료 유출과 구별이 안 되는 화면은 그 자체로 문제다.
「괜찮다」고 설명하는 것으로 끝내면 안 된다 — 화면이 스스로 말하게 만들어야 한다.

### 1.22 고친 것

#### `0006_sample_marker.sql`

```sql
alter table organizations add column if not exists sample_seeded_at timestamptz;
```

`null` 이면 실제 자료, 값이 있으면 샘플. 시딩할 때 켜고 초기화할 때 끈다.

권한은 함수로 열었다 — `organizations_update` 정책은 owner 만 허용하는데,
시딩은 담당자도 할 수 있어야 한다.

```sql
create or replace function fn_mark_sample(p_org uuid, p_on boolean) …
  if app_role(p_org) not in ('owner','manager','staff') then raise …
```

> **함정** · `create or replace view` 는 **뒤에만** 열을 붙일 수 있다.
> 중간에 끼우면 `42P16 cannot change name of view column`. 새 열을 맨 끝으로 옮겨 해결.

#### 화면

```
 ┌──────────────────────────────────────┐
 │ [가 회사 ▾]  [샘플 데이터]           │  ← 배지
 │ me@example.com · 가 회사             │  ← 누구로 · 어디를
 │ 기준 2026-09-08 · …                  │
 └──────────────────────────────────────┘
```

회사 이름은 이미 있었지만 **로그인 계정**이 없었다. 「내가 지금 누구지」에 답할 수 없으면
계정을 바꿨는지조차 확인이 안 된다.

#### 로그아웃 · 남은 쿠키

```ts
await supabase.auth.signOut();
response.cookies.delete(ORG_COOKIE_NAME);   // ← 없던 줄
```

세션만 지우고 「보고 있는 회사」 쿠키는 남기고 있었다.
`getCurrentOrg` 가 소속을 확인하므로 **악용은 안 되지만**, 로그아웃이 흔적을 남길 이유가 없다.

### 1.23 테스트 — 격리 규칙을 못 박았다

`test/tenancy.test.tsx` 6개. 실제 차단은 RLS 가 하지만, **그 앞단인 「어느 회사를 고르는가」**
가 틀리면 RLS 앞에서 이미 샌다.

| 검증 | 기대 |
|---|---|
| 쿠키가 내 소속 | 그 회사 |
| 쿠키가 **남의 회사** | 무시하고 내 첫 회사 |
| 소속이 없음 | `null` → 온보딩 |
| 헤더 | 계정 · 회사명 표시 |
| 샘플 회사 | 배지 있음 |
| 실제 회사 | 배지 없음 |

웹 29 통과 (23 → 29).

---

## 2부 · 쉽게 보기

### 신고: 「다른 계정인데 똑같이 보여요」

당연히 의심할 상황이다. 그래서 **말로 답하지 않고 창고를 열어 세어 봤다.**

```
                창고 ①        창고 ②        창고 ③
                ┌────────┐   ┌────────┐   ┌────────┐
   계정 A ─열쇠─▶│ 106상자│   │ 106상자│   │ 106상자│
   계정 B ──────────열쇠──▶   │        │   │        │
   계정 C ─────────────────열쇠──▶      │   │        │
                └────────┘   └────────┘   └────────┘

   상자가 겹치는가?  →  0개.  창고 셋은 완전히 별개다.
```

**상자 안에 든 것이 같았을 뿐이다.** 셋 다 같은 「샘플 세트」를 주문했으니까.

```
   샘플 세트 = 정해진 내용물
   ┌───────────────────────────────┐
   │ 채권 106 · 고정비 9 · 지출 55 │
   │ 법인A · 법인B                 │  ← 항상 이 이름
   └───────────────────────────────┘
        │           │           │
        ▼           ▼           ▼
      창고①       창고②       창고③     ← 그래서 똑같이 보인다
```

### 열쇠 검사도 다시 해 봤다

```
   열쇠 없이 창고 열기       →  ✕  아무것도 안 나옴
   RLS 꺼진 창고 있나        →  0개
   자물쇠 없는 창고 있나     →  0개
```

### 그래도 고쳤다 — 「똑같아 보이는 것」이 문제니까

문제가 없다고 설명하는 것으로 끝내면, 다음에 **진짜로 샜을 때도 똑같이 보인다.**

```
   전                            후
   ┌──────────────┐             ┌────────────────────────────┐
   │ 가 회사   ▾  │             │ 가 회사 ▾   [샘플 데이터]  │
   │              │             │ me@example.com · 가 회사   │
   └──────────────┘             └────────────────────────────┘
      ▲                                  ▲
   누구로 로그인했는지            계정도 · 샘플인지도 보인다
   화면에 없었다
```

- **[샘플 데이터]** 배지 → 「똑같이 보이는 게 정상」임을 화면이 말한다
- **계정 이메일** → 계정을 바꿨는지 눈으로 확인된다
- 실제 자료를 넣으면 배지가 사라진다

### 로그아웃할 때 흔적 지우기

```
   전:  로그아웃 → 세션만 지움,  「보던 회사」 쪽지는 남음
   후:  로그아웃 → 세션 지움  +  쪽지도 버림
```

앞사람 쪽지를 다음 사람이 써도 **소속을 확인하니 열리진 않았다.**
그래도 남길 이유가 없어서 지운다.

---

## 이전 작업 수정 이력 (이어서 3)

| 언제 한 것 | 무엇을 고쳤나 | 왜 |
|---|---|---|
| 09-07 · 로그아웃 | 회사 쿠키도 지우도록 | 세션만 지우고 있었다. 악용은 안 됐지만 남길 값이 아니다 |
| 09-07 · 헤더 | **로그인 계정** 표시 추가 | 「내가 지금 누구지」에 답할 수 없었다 |
| 09-07 · 샘플 시딩 | 샘플 표시(`sample_seeded_at`) 기록 | 샘플과 실제 자료가 화면에서 구별되지 않았다 |
| 09-06 · `v_my_organizations` | 새 열 추가 (맨 뒤에) | 배지 근거를 헤더까지 넘기려면 필요 |

### 확인했지만 안 고친 것

| 무엇 | 왜 |
|---|---|
| RLS 정책 | 구멍 없음 — 미적용 0 · 정책 없음 0 |
| 익명 접근 | 전부 `[]` |
| 회사 고르기 | 남의 회사 쿠키는 이미 무시하고 있었다 (테스트로 못 박음) |
| 샘플 법인명을 회사마다 다르게 | 샘플의 뜻이 흐려진다. 배지로 푸는 쪽이 낫다 |
